<div data-role="page" id="admin">
	<div data-role="header">
		<h1>Admin Page</h1>
	</div>

	<div data-role="content">
		<p> Users </p>
		<% @active_users.each do |user| %>
		<li> <%= user.name %> </li>
	<% end %>
	</div>
	<%= render :partial => "shared/footer", :locals => {:viewing => :admin} %>
</div>
